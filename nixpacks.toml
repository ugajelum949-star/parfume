# Node + sharp native deps
[phases.setup]
nixPkgs = ["nodejs_22", "python311", "gcc"]

[phases.install]
cmds = ["npm ci --prefer-offline"]

[start]
cmd = "node .next/standalone/server.js"

[env]
PORT = "3000"
HOSTNAME = "0.0.0.0"
NODE_ENV = "production"
